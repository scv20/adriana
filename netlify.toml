[build]
  command = "npm run build"
  publish = "dist"

# Single-page app: serve index.html for every route so deep links like
# /birth-of-a-parent and /birth-of-a-parent/ritual work on refresh.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
